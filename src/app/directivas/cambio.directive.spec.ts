import { CambioDirective } from './cambio.directive';

describe('CambioDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioDirective();
    expect(directive).toBeTruthy();
  });
});
